# Copy to `.env` and adjust, or export in your shell.
REDIS_URL=redis://127.0.0.1:6379

# Matcher process (order book + queue consumer)
MATCHER_HTTP_ADDR=0.0.0.0:4001

# API process (stateless HTTP + WebSocket)
MATCHER_HTTP_URL=http://127.0.0.1:4001
API_ADDR=0.0.0.0:3000
